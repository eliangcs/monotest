{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0",
    "mocha": "^5.2.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --stream",
    "bump": "lerna version --exact --force-publish=AdditionModule,SubtractionModule"
  },
  "version": "1.0.0"
}
